* TODO: Update! *

 # Make sure your WC is updated, pointing at trunk and hasn't got any local modifications.
 # Get the latest revision number (R) from the repository to be able to calculate the future release revision, R+3.
 # Make sure everything bilds correctly by executing "mvn clean package assembly:assembly".
 # Update the file "changes.txt" and add a row for the release containing the future revision (R+3) and the version/date.
 # Update the web page web/download.php and include a link to the new, to be released, version.
 # Commit the updated files and log "Prepared for release x.y.z.".
 # Run "mvn clean release:prepare".
 # Copy the artifacts from the target directory to the ftp servers at berlios.de and sventon.org.
 # Create a new release at berlios.de.
 # Upload the updated web pages to sventon.org.