Set the system property `sventon.baseURL` to specify the base URL. This is useful if the container running sventon is behind a web server e.g. Apache.

Example:
```
-Dsventon.baseURL=http://your-server/svn
```